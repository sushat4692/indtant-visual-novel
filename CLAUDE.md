# CLAUDE.md

ブラウザだけで動く、ビジュアルノベル作成・再生ツール。独自 DSL でシーンを書き、
エディタで編集・プレビューし、プレイヤーで再生する。

## このプロジェクトについて（背景・目的）

「サクッと作って、特定の相手に手軽に渡せる」ビジュアルノベルを作るためのツール。
公開プラットフォームに投稿して不特定多数に晒すのではなく、身内・限定された相手へ
配布することを前提にしている。

動機:
- 既存ツール（Ren'Py 等）は高機能だが、インストールや環境構築が面倒で手軽さに欠ける
- 作ったものが公開の場に晒されるプレッシャーを避けたい／下書き・制作中のものを守りたい
- 誰に渡すかを自分でコントロールしたい

想定ユーザー: とりあえず物語を作って、限られた相手にサッと共有したい人。

## 大事にしている思想（判断に迷ったらここに従う）

優先度の高い順:

1. **手軽さ・シンプルさ最優先** — 機能を盛りすぎない。学習コストを上げる追加には
   慎重に。「これは手軽さを損なわないか？」を常に問う。
2. **ローカルファースト / オフライン** — データはユーザーの手元（ブラウザ内）に留まる。
   サーバーに送らない・保管しない。オフラインでも動く。
3. **非公開・限定共有** — 不特定多数に公開される導線は作らない。配布相手をユーザーが
   選べる形にする。再生専用の書き出しはパスワードで保護できる。
4. **規定アセット前提で軽量に** — 画像・音声は同梱のビルトインを基本とし、プロジェクトを
   軽く保つ。これにより配布が容易になり、URL 共有も射程に入る。

## やらないこと（アンチゴール）

- 作品が一覧公開される投稿プラットフォーム（itch.io 的なもの）にはしない
- アカウント登録・ログイン・クラウド保存を必須とするサービスにはしない
- 学習コストの高い多機能エンジン路線には進まない（手軽さ最優先の帰結）

## 配布の考え方

- 基本はファイル配布（編集可能版／再生専用・暗号化版）。
- 規定アセットのみ・短〜中編なら、将来的に URL(#ハッシュ)共有も選択肢。`#` 以降は
  サーバーに送信されないため非公開性を保てる。圧縮前提で、日本語テキスト数百行程度
  （短〜中編）が現実的な目安。長編やリンク長制限のある経路ではファイル配布を使う。

## 開発の約束事

- **機能・DSL 記法を追加／変更したら、必ず対応するテストを追加する。** テストなしで
  完了とみなさない。追加後は `npm run test` が全て green であること。
- 既存ファイルの慣例（型・命名・コメントの粒度）に合わせる。
- UI 文言・ドキュメントは日本語。
- 後方互換のためのシムや未使用コードは残さない。

## コマンド

- `npm run dev` — 開発サーバー
- `npm run build` — 型チェック + ビルド
- `npm run test` — テスト一括実行（vitest）
- `npm run test:watch` — テスト監視実行
